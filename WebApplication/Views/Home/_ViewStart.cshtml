@{
    // u can add this folder in every file in view
    // this folder going to override the view start folder in views
    Layout = "_Layout";

    // check the user role to select which layout

    //  if (User.IsInRole("Admin"))
    //{
    //  Layout = "_AdminLayout";
    //}
    //else
    //    {
    //      Layout = "_NonAdmin";
    //}
}
